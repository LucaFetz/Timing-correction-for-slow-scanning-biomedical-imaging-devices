function corrected_measures = Frame_correction_2D(measures,spline_order,noise,noise_snr,lambda)
%Function that returns time corrected measurements from 2D images
%input: measurements to correct[Nx*Ny], order for spline interpolation[1 or
%3], noise[0 for no, 1 for yes], noise_snr [dB], lambda for regulation
%thuning
%Output: corrected measurements

%% define parameters for the simulation
[Nx, Ny] = size(measures);


end
